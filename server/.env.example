# Вариант 1: отдельные переменные (надёжно подхватываются)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=твой_пароль_postgres
DB_NAME=shareholder_voting
DB_SSL=false

# Вариант 2: одна строка (если удобнее)
# DATABASE_URL=postgres://postgres:твой_пароль@localhost:5432/shareholder_voting

JWT_SECRET=super_secret_key_change_me
PORT=4000

