{
  "name": "shareholder-voting-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "dev:all": "npm run dev:server --workspace server & npm run dev --workspace client"
  }
}
