{
  "name": "shareholder-voting-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "setup": "node scripts/setup-env.mjs",
    "env:docker": "node scripts/apply-docker-env.mjs",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:wait": "node scripts/wait-for-postgres.mjs",
    "start:docker": "npm run env:docker && npm run db:up && npm run db:wait && npm run dev:all",
    "dev": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "dev:all": "concurrently -n server,client -c yellow,cyan \"npm run dev:server\" \"npm run dev\"",
    "start:local": "npm run setup && npm run dev:all"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
